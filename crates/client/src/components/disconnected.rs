use ratatui::{
    layout::{Position, Rect},
    style::{Color, Style},
    text::Paragraph,
    widgets::{Block, Borders, Clear},
    Frame,
};

pub struct DisconnectedComponent;

impl DisconnectedComponent {
    pub fn render(frame: &mut Frame) {
        // Code for rendering disconnected state goes here
    }
}

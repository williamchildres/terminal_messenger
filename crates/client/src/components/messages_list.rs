use crate::app::App;
use ratatui::{
    layout::{Position, Rect},
    style::{Color, Style},
    widgets::{Block, Borders},
    Frame,
};

pub struct MessagesListComponent;

impl MessagesListComponent {
    pub fn render(frame: &mut Frame) {
        // Code for rendering messages list goes here
    }
}

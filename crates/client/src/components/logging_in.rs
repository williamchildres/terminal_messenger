use crate::app::App;
use ratatui::{
    layout::{Position, Rect},
    style::{Color, Style},
    widgets::{Block, Borders, Clear},
    Frame,
};

pub struct LoggingInComponent;

impl LoggingInComponent {
    pub fn render(frame: &mut Frame) {
        // Code for rendering logging in state goes here
    }
}

use crate::app::App;
use ratatui::{
    layout::{Position, Rect},
    style::{Color, Style},
    widgets::{Block, Borders},
    Frame,
};

pub struct HeaderComponent;

impl HeaderComponent {
    pub fn render(frame: &mut Frame) {
        // Code for rendering header goes here
    }
}
